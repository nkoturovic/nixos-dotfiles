# Open items (ordered)

1. **Activate 2.23.0** — needs the operator green light. `home-manager
   switch --flake /home/kotur/personal/nixos-dotfiles#kotur` (rollback
   anchor gen 134) → gateway healthz → `claude-multi doctor` Ready →
   confirm a fresh 1M-class session reports window 800000 / trigger 702000.
   Done-criteria: doctor Ready at the new generation; zero-live census as
   before.
2. **WS4 (deferred, main build task)** — single-model mode for any model +
   `--no-subagents` + local Qwen3.8-Flash-Next adapter. Spec:
   `HANDOFF-ASTRA-BATCH.md` §6a + plan file
   `/home/kotur/.claude/plans/iridescent-sauteeing-lampson.md`.
3. **Meta muse-spark live probe** — approval-gated per call (auth,
   thinking-always-on, high/xhigh efforts, `refusal` stop_reason); then flip
   the Meta listing descriptor attempt→verified.
4. **Astra near-limit validation (optional)** — bounded larger-context probe,
   approval-gated. The 800K ceiling mitigates the uncertainty; a probe would
   turn mitigation into evidence.
5. **Remaining presets (optional)** — `muse-direct`,
   `muse-contributor-direct`, `qwen-local-direct` (needs WS4).
6. **Holistic TUI reconsideration** — operator's standing ask for the model
   driving next (especially composition/picker ergonomics).
